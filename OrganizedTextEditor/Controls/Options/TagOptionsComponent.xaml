<UserControl x:Class="OrganizedTextEditor.Controls.Options.TagOptionsComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrganizedTextEditor.Controls.Options"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="addTagButton" HorizontalAlignment="Left" Padding="3" Margin="5 0 0 5">Add Tag</Button>

        <Border BorderBrush="Black" BorderThickness="1" Grid.Row="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" MaxHeight="200">
                <StackPanel x:Name="tagsStackPanel" MinHeight="100" HorizontalAlignment="Left" MinWidth="200">
            
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
